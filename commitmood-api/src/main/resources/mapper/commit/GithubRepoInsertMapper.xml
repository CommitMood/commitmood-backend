<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.commitmood.domain.commit.repository.GithubRepoInsertMapper">

    <!-- GitHub 저장소 정보 저장 -->
    <insert id="insert" parameterType="com.ssafy.commitmood.domain.github.entity.GithubRepo"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO github_repo (
            user_account_id,
            github_repo_id,
            github_repo_name,
            github_repo_full_name,
            default_branch,
            description,
            github_repo_url,
            is_private,
            last_synced_at,
            created_at,
            updated_at
        ) VALUES (
                     #{userAccountId},
                     #{githubRepoId},
                     #{githubRepoName},
                     #{githubRepoFullName},
                     #{defaultBranch},
                     #{description},
                     #{githubRepoUrl},
                     #{isPrivate},
                     #{lastSyncedAt},
                     NOW(),
                     NOW()
                 )
    </insert>

</mapper>