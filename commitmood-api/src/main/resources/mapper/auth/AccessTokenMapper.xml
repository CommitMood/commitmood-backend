<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.commitmood.security.mapper.AccessTokenMapper">

    <resultMap id="AccessTokenResultMap" type="com.ssafy.commitmood.domain.auth.entity.AccessToken">
        <id property="id" column="id"/>
        <result property="userAccountId" column="user_account_id"/>
        <result property="accessToken" column="access_token"/>
        <result property="tokenType" column="token_type"/>
        <result property="scope" column="scope"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO access_token (user_account_id, access_token, token_type, scope, expires_at)
        VALUES (#{userAccountId}, #{accessToken}, #{tokenType}, #{scope}, #{expiresAt})
    </insert>

    <select id="findByUserAccountId" resultMap="AccessTokenResultMap">
        SELECT *
        FROM access_token
        WHERE user_account_id = #{userAccountId}
    </select>

    <delete id="deleteByUserAccountId">
        DELETE FROM access_token
        WHERE user_account_id = #{userAccountId}
    </delete>

</mapper>
